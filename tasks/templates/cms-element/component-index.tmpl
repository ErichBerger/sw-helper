import template from './sw-cms-el-{{.KebabName}}.html.twig';
import './sw-cms-el-{{.KebabName}}.scss';

Shopware.Component.register('sw-cms-el-{{.KebabName}}', {
    template,
    mixins: [
        'cms-element'
    ],
    created() {
        this.createdComponent();
    },

    methods: {
        createdComponent() {
            this.initElementConfig('{{.KebabName}}');

        }
    }

});